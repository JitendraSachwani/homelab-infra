docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin

docker_apt_key_url: https://download.docker.com/linux/ubuntu/gpg
docker_arch_map:
  x86_64: "amd64"
  aarch64: "arm64"
docker_apt_repo: >
  deb [arch={{ docker_arch_map[ansible_architecture] }} signed-by=/etc/apt/keyrings/docker.gpg]
  https://download.docker.com/linux/ubuntu
  {{ ansible_distribution_release }} stable

docker_users:
  - iac

docker_daemon_config:
  log-driver: json-file
  log-opts:
    max-size: "100m"
    max-file: "3"
